POSTGRES_USER=umanni
POSTGRES_PASSWORD=umanni-local
DATABASE_URL=postgresql://umanni:umanni-local@db:5432/umanni_development
DELIVERY_DATABASE_URL=postgresql://umanni:umanni-local@db:5432/umanni_production
# Supply RAILS_MASTER_KEY privately at delivery runtime (for example, through the shell).
# Do not store it in tracked files or replace encrypted Rails credentials with SECRET_KEY_BASE.
